providers = ["php", "node"]

[phases.install]
cmds = [
    "composer install --no-dev --optimize-autoloader --no-interaction",
    "npm ci"
]

[phases.build]
cmds = [
    "npm run build",
    "chmod +x start.sh"
]

[start]
cmd = "bash start.sh"
